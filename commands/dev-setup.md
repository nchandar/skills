---
description: Establish lightweight documentation and artifact conventions for the dev-* workflow.
---

# Dev Setup

Use the `dev-setup` skill.

Request:
$ARGUMENTS

Instructions:
- Detect existing conventions first.
- Ask only about unresolved gaps.
- Keep setup lightweight unless the user explicitly wants persistent scaffolding.
