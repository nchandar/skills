---
description: Create or revise a dev-* skill using the repository authoring standard.
---

# Dev Write Skill

Use the `dev-write-skill` skill.

Request:
$ARGUMENTS

Instructions:
- Check whether an existing skill should be updated before creating a new one.
- Define the skill's triggers, boundaries, and handoffs clearly.
- Keep `SKILL.md` concise and split deeper material only when useful.
- Keep the command name equal to the skill name.
- Update README and plugin metadata when the public catalog changes.
