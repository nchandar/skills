---
description: Turn an approved design or clear requirement into an executable implementation plan.
---

# Dev Plan

Use the `dev-plan` skill.

Request:
$ARGUMENTS

Instructions:
- Map affected areas and files before writing tasks.
- Break work into small verifiable slices.
- Include test, review, and verification gates.
- Hand off to `dev-execute` when the plan is ready.
