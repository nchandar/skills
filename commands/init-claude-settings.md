---
description: Bootstrap Niranjan's personal Claude Code settings on this machine (statusline, theme, voice, permissions).
---

# Init Claude Settings

Use the `init-claude-settings` skill.

Request:
$ARGUMENTS

Instructions:
- Write the statusline script to ~/.claude/statusline-command.sh.
- Merge the required settings into ~/.claude/settings.json without removing existing keys.
- Confirm what was written when done.
