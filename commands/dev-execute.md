---
description: Execute an approved plan task by task and finish with review and verification.
---

# Dev Execute

Use the `dev-execute` skill.

Request:
$ARGUMENTS

Instructions:
- Carry the approved plan forward task by task.
- Use `dev-tdd` for implementation slices.
- Use `dev-debug` first when root cause is unknown.
- End with `dev-review` and `dev-verify`.
